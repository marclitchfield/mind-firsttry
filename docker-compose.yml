version: '2'
services:
  mind-api:
    container_name: mind-api
    build: ./api
    ports:
    - "5000:9051"
    volumes:
    - apilog:/var/log
    environment:
    - MIND_DGRAPH_URL=http://mind-db:8080
    links:
    - mind-db
  mind-db:
    container_name: mind-db
    entrypoint: dgraph
    ports:
    - "8080:8080"
    image: dgraph/dgraph
    volumes:
    - datavolume:/data
volumes:
  apilog: {}
  datavolume: {}
